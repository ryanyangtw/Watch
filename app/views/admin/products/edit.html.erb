<div class="">
  <h1>修改產品</h1>

  <%= render partial: 'shared/errors', locals: {obj: @product} %>

  <%= simple_nested_form_for @product, html: { multipart: true }, :url => admin_product_path(@product) do |f| %>

  <%= render(:partial=>'form', :locals=>{:f=>f})%>
 
  
  <div class="form-group">
    <%= f.submit '修改', class: "btn btn-primary btn-lg" %>

  </div>

  <% end %>
</div>