<div class="">
  <h1>新增產品</h1>

  <%= render partial: 'shared/errors', locals: {obj: @product} %>

  <%= simple_nested_form_for @product, html: { multipart: true }, :url => admin_brand_products_path(@brand) do |f| %>

  <%= render(:partial=>'form', :locals=>{:f=>f})%>
 
  
  <div class="form-group">
    <%= f.submit '新增', class: "btn btn-primary btn-lg" %>
    
  </div>

  <% end %>
</div>