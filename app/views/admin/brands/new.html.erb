<div class="">
  <h1>New Brand</h1>

  <%= render(partial: 'shared/errors', locals: {obj: @brand}) %>
  
  <%= simple_nested_form_for @brand, html: { multipart: true},:url => admin_brands_path do |f| %>


  <%= render(:partial=>'form', :locals=>{:f=>f})%>
 
  
  <div class="form-group">
    <%= f.submit '新增', class: "btn btn-primary btn-lg" %>
  </div>

  <% end %>
</div>